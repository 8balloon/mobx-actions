{
  "name": "mobx-actions",
  "version": "6.1.0",
  "description": "Actions for Mobx. Dispatch once, listen anywhere.",
  "main": "lib/mobx-actions.js",
  "repository": "https://github.com/8balloon/mobx-actions",
  "author": "Ethan Gregory Clark",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "babel src/mobx-actions.js --out-file lib/mobx-actions.js --presets=env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "mobx": "^4.5.0"
  }
}
